{
  "name": "pyramid-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "wrangler deploy",
    "deploy-frontend": "vite build && wrangler pages deploy dist --project-name=pyramid-5050"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260131.0",
    "@sveltejs/vite-plugin-svelte": "^3.1.0",
    "svelte": "^4.2.12",
    "vite": "^5.2.0",
    "wrangler": "^4.61.1"
  },
  "dependencies": {
    "panzoom": "^9.4.3"
  }
}
